# Ativa o motor de reescrita de URL do Apache
RewriteEngine On

# Evita que a regra de reescrita se aplique a diretórios ou arquivos existentes.
# Isso é crucial para que os links de CSS, JS e imagens continuem funcionando.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona todas as outras requisições para o index.php,
# passando a URL original como um parâmetro 'url'.
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]